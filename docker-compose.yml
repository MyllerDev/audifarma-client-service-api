version: '3.8'

services:

  client-service:

    build: .

    container_name: client-service

    ports:
      - "8080:8080"

    environment:
      SPRING_PROFILES_ACTIVE: docker

    restart: always